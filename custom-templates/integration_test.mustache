//! Autogenerated integration tests for the Cerebras Rust SDK
//! This file is generated from the OpenAPI spec using custom templates.

use cerebras_rs::prelude::*;

{{#operations}}
#[tokio::test]
async fn test_{{operationId}}() {
    let client = Client::from_env().expect("API key not set");
    // TODO: Fill in example data for parameters
    {{#allParams}}
    // let {{paramName}}: {{dataType}} = /* example or default value */ unimplemented!();
    {{/allParams}}
    let result = client.{{operationId}}(
        {{#allParams}}{{paramName}}, {{/allParams}}
    ).await;
    assert!(result.is_ok(), "API call failed: {:?}", result.err());
    // Optionally, assert on response fields
}
{{/operations}}
